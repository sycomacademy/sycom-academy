# azd writes per-environment state here, including secret parameter values.
# Everything is ignored except the deployment plan, which is the source of truth
# for the azure-prepare / azure-validate / azure-deploy workflow.
*
!.gitignore
!deployment-plan.md
