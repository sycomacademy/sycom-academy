**/node_modules
.git

**/dist
**/build
**/.next
**/.nuxt
**/.output
**/.svelte-kit
**/.astro
**/.turbo
.turbo

**/.wrangler
**/.alchemy
**/.expo
**/.vercel
*.log

Dockerfile
**/Dockerfile
docker-compose.yml

# Deployment tooling is not needed in the image, and the context is uploaded to
# ACR for remote builds
infra
.azure

# Agent and editor config. .claude/skills contains symlinks to directories, which
# the remote build context packer cannot follow.
.claude
.agents
.cursor
.github
.vscode

# Secrets stay out of image layers; runtime env comes from compose env_file,
# build-time public values come from compose build args
**/.env
**/.env.*
!**/.env.example

local.db
local.db-*
.data
