# https://www.robotstxt.org/robotstxt.html
#
# This host is the application, not the marketing site. The auth pages are the
# only thing worth crawling, so everything behind a session is closed off here.
# Those routes also carry a `noindex` meta tag: robots.txt only stops the crawl,
# it does not remove an already-indexed page.

User-agent: *
Disallow: /dashboard
Disallow: /api/
